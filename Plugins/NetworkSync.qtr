func read_manifest(path: string):
    # Parse JSON or INI for plugin entries
    return JSON.parse(File.read(path))

func download_module(url: string):
    let code = Http.get(url)
    return LibrarySystem.compile(code)
